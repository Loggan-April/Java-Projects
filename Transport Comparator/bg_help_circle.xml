package com.example.transportcostcomparator;

import android.app.Activity;
import android.content.ComponentName;
import android.content.Context;
import android.content.ServiceConnection;
import android.hardware.camera2.params.MultiResolutionStreamConfigurationMap;
import android.media.MediaPlayer;
import android.os.Bundle;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;
import android.content.Intent;
import android.os.IBinder;
import android.view.View;
import android.widget.ArrayAdapter;
import android.widget.Button;
import android.widget.Spinner;
import android.widget.TextView;
import android.widget.Toast;

public class MainActivity extends AppCompatActivity {

    private MusicService musicService;
    private boolean isBound =false;
    private MediaPlayer listenPlayer;

    private final ServiceConnection connection = new ServiceConnection() {
        @Override
        public void onServiceConnected(ComponentName name, IBinder service) {
            MusicService.MusicBinder binder = (MusicService.MusicBinder) service;
            musicService = binder.getService();
            isBound = true;
        }

        @Override
        public void onServiceDisconnected(ComponentName name) {
            isBound = false;
        }
    };
    private MediaPlayer mediaPlayer;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_main);
        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {
            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
            return insets;
        });

        bindService(new Intent(this, MusicService.class), connection, Context.BIND_AUTO_CREATE);

        Button btnHome = findViewById(R.id.btnHome);
        Button btnDetails = findViewById(R.id.btnDetails);
        Button btnReport = findViewById(R.id.btnReport);
        Button btnListen = findViewById(R.id.btnListen);
        TextView btnHelp = findViewById(R.id.btnHelp);


        btnHome.setOnClickListener(v -> {
            if (!(this instanceof MainActivity)){
                startActivity(new Intent(this, MainActivity.class));
            }
        });


        btnDetails.setOnClickListener(v -> {
            Intent intent =
                    new Intent(MainActivity.this, InputActivity.class);
            startActivity(intent);
        });

        btnHelp.setOnClickListener(v -> {
            Intent intent =
                    new Intent(MainActivity.this, HelpActivity.class);
            startActivity(intent);
        });

        btnReport.setOnClickListener(v -> {
            Intent intent =
                    new Intent(MainActivity.this, ResultsActivity.class);
            startActivity(intent);
        });

        Button btnStartCalculation = findViewById(R.id.btnStartCalculation);
        btnStartCalculation.setOnClickListener(v -> {
            Intent intent =
                    new Intent(MainActivity.this, InputActivity.class);
            startActivity(intent);
        });

        btnListen.setOnClickListener(v -> {
            if (isBound && musicService != null) {
                musicService.pauseMusic();
            }

            listenPlayer = MediaPlayer.create(this, R.raw.honorable);
            if (listenPlayer != null) {
                listenPlayer.setOnCompletionListener(mp -> {
                    if (isBound && musicService != null) {
                        musicService.resumeMusic();
                    }
                    mp.release();
                });
                listenPlayer.start();
            } else if (isBound && musicService != null) {
                // audio file failed to load — don't leave the music paused forever
                musicService.resumeMusic();
            }
        });

        Button btnExit = findViewById(R.id.btnExit);
        btnExit.setOnClickListener((view ->{
            stopService(new Intent(this, MusicService.class));
            finishAffinity();
            System.exit(0);
        }));



    }

    @Override
    protected void onDestroy() {
        super.onDestroy();
        // Prevent memory leaks by releasing the MediaPlayer if the activity is destroyed
        if (mediaPlayer != null) {
            mediaPlayer.release();
            mediaPlayer = null;
        }
    }
}


