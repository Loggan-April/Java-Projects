package com.example.transportcostcomparator;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.widget.Button;
import android.widget.EditText;
import android.widget.Spinner;
import android.widget.TextView;
import android.widget.Toast;

import androidx.activity.EdgeToEdge;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

public class InputActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_input);
        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.input), (v, insets) -> {
            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
            return insets;
        });

        Button btnHome = findViewById(R.id.btnHome);
        Button btnReport = findViewById(R.id.btnReport);
        TextView btnHelp = findViewById(R.id.btnHelp);

        btnReport.setOnClickListener(v -> {
            Intent intent = new Intent(InputActivity.this, ResultsActivity.class);
            startActivity(intent);
        });

        btnHome.setOnClickListener(v -> {
            Intent intent = new Intent(InputActivity.this, MainActivity.class);
            startActivity(intent);
        });

        btnHelp.setOnClickListener(v -> {
            Intent intent = new Intent(InputActivity.this, HelpActivity.class);
            startActivity(intent);
        });

        EditText etDistanceTravelled = findViewById(R.id.etDistanceTravelled);
        EditText etCostPerKm = findViewById(R.id.etCostPerKm);
        EditText etTravelDays = findViewById(R.id.etTravelDays);
        Spinner spinnerTransportMode = findViewById(R.id.spinnerTransportMode);

        Button btnCalculate = findViewById(R.id.btnCalculate);
        btnCalculate.setOnClickListener(v -> {

            String distanceText = etDistanceTravelled.getText().toString().trim();
            String costText = etCostPerKm.getText().toString().trim();
            String daysText = etTravelDays.getText().toString().trim();
            String mode = spinnerTransportMode.getSelectedItem().toString();

            if (distanceText.isEmpty() || costText.isEmpty() || daysText.isEmpty()) {
                Toast.makeText(this, "Please fill in all fields", Toast.LENGTH_SHORT).show();
                return;
            }

            double distancePerDay = Double.parseDouble(distanceText);
            double costPerKm = Double.parseDouble(costText);
            int travelDays = Integer.parseInt(daysText);

            Transport transport = new Transport(mode, distancePerDay, costPerKm, travelDays);

            Intent intent = new Intent(InputActivity.this, ResultsActivity.class);
            intent.putExtra("transport", transport);
            startActivity(intent);
        });
    }
}